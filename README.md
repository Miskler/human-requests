# human-requests

```
# из папки test_server/
make serve       # форграунд (Ctrl+C чтобы остановить)
# или
make serve-bg    # фон, создаст .server.pid
make smoke       # лёгкий опрос /base, /api/base, /api/challenge
make stop        # стопнуть фоновый
make test        # pytest
```
